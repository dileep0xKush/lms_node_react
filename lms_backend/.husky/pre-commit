#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running ESLint check..."

npm run lint

if [ $? -ne 0 ]; then
  echo "❌ Commit blocked — fix lint errors and try again."
  exit 1
fi

echo "✅ Lint passed — commit allowed 🎉"
